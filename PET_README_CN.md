# PET词汇精读

这是基于开源项目 Recall 改造的 PET 词汇学习 PWA，采用 MIT 许可证。

## 已完成功能

- 固定账号登录：管理员 `FREE1`、`FREE2`，普通用户 `RUN1` 至 `RUN10`，初始密码均为 `123456`。
- 每个账号的学习进度、错词和精读资料在当前浏览器中独立保存。
- 30 个重点 B1/PET 单词，包含英式音标、中文词义和 PET 难度原创例句；例句中的目标词会高亮显示。
- 按主题组织学习顺序，不按字母排序；自然拼读支持分音节播放和当前音节高亮。
- 调用设备系统的英式英语语音朗读单词和例句。
- “不认识、有点熟、认识”三档学习记录。
- 自动生成错词本，学习数据保存在浏览器本地。
- 可在“30 词重点学习库”和“2354 词 CEFR B1 扩展库”之间切换、搜索并查看词量。
- 精读分为“阅读精读”和“听力精读”两个独立子模块，切换后显示不同内容，保存的资料也不会混用。
- 阅读精读内置一篇 PET 难度短文，支持导入并保存 TXT、DOCX、PDF 提取出的文字。
- 听力精读接入 Cambridge 官网当前公开的 B1 Preliminary for Schools 样题音频和原文下载入口，也可导入自己的音频与听力原文。
- 精读支持朗读全文、逐句朗读和 0.75x、1x、1.25x、1.5x 倍速；听力音频也可调速。
- 听力原文的每句话可以配置原版音频句首、句尾时间；配置按账号和资料自动保存。
- 支持原版音频单句定位、单句循环，以及自动挖空、完整句子核对的听写训练。
- 支持导出和恢复 JSON 学习备份。
- PWA 离线缓存，适配 iPhone 和华为 MatePad Air。

## 本地运行

需要 Node.js 22 或更高版本、pnpm 10 或更高版本。

```powershell
pnpm install --frozen-lockfile
pnpm dev
```

浏览器打开 `http://localhost:5173`。

## 检查命令

```powershell
pnpm build
pnpm exec eslint src/pet src/main.tsx
pnpm exec playwright test --config=playwright.pet.config.ts
```

## 当前边界

- 发音来自 iPhone 或华为设备自带的系统语音，不包含第三方版权音频。
- 当前账号是静态网页中的本地账号门禁，不是服务器端安全认证。它适合家庭或小范围试用，不能抵御懂开发工具的人查看前端代码。
- 学习数据和导入后的文档文字只存在当前浏览器。更换浏览器或清除数据前，请在“我的”页面导出备份。
- 音频文件只在当前页面会话中使用，刷新后需重新选择；扫描版 PDF 暂不支持 OCR。
- Cambridge 官方音频采用官网在线播放，不复制到应用仓库；离线时需导入已下载的个人音频。
- PDF/TXT 原文没有自带时间轴，需要首次使用时人工记录每句话的句首和句尾；之后会自动保存。
- 本地预览不注册 PWA 缓存，确保开发更新可以直接刷新；正式发布版本仍支持离线缓存。
- 2354 词扩展库是开放许可的 CEFR B1 词表，不是 Cambridge 官方 PET 完整词表，来源和许可见 `public/data/ATTRIBUTION.md`。
