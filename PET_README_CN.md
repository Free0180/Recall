# PET词汇精读

这是基于开源项目 Recall 改造的 PET 词汇学习 PWA，采用 MIT 许可证。

数据库同步和语音选择的新增实现及配置步骤见 [免费数据库与音频配置](docs/PET_CLOUD_AUDIO_CN.md)。线上同步需先执行数据库迁移并发布前端，不能仅凭代码存在判断已上线。

## 已完成功能

- 学习页采用 7 个自然日为一个周期，从首次启用周期的本机日期开始；第 1 至 5 天学习新词，第 6、7 天分批复习。
- 新建计划默认每天 10 个新词，使用 30 词重点学习库；下周期词量可在学习页设置，已有账号保存的数量不覆盖。2354 词扩展库仍用于查询和朗读，暂未纳入每日计划；当前重点库按 10 词/天只能安排 3 天新词，其余新词日按实际剩余词量显示。
- 第 6 天按前 5 天实际标为“不认识”或“有点熟”的词生成复习计划，优先分配“不认识”，两批不重复，词量差不超过 1；分配后保持稳定。
- 当天所有单词打分后解锁每日听写，拼写正确才计为完成；大小写和首尾空格不影响判定。
- 当天单词完成后，使用原创故事素材在本机自动组文，覆盖全部目标词并提供 PET/B1 语法讲解。无需联网或 AI 服务；同一词表与日期得到稳定内容，故事素材会复用，文章和听写记录随学习备份保存。
- 今日文章和往期文章支持 0.75x、1x、1.25x、1.5x、2x 朗读与停止播放，播放中切换速度从当前句重新开始。历史 AI 服务代码暂未启用，不影响离线文章功能。

- 固定账号登录：管理员 `FREE1`、`FREE2`，普通用户 `RUN1` 至 `RUN10`，初始密码均为 `123456`。
- 每个账号的学习进度、错词和精读资料在当前浏览器中独立保存。
- 30 个重点 B1/PET 单词，包含英式音标、中文词义和 PET 难度原创例句；例句中的目标词会高亮显示。
- 按主题组织学习顺序，不按字母排序；自然拼读支持分音节播放和当前音节高亮。
- 调用设备系统的英式英语语音朗读单词和例句。
- “不认识、有点熟、认识”三档学习记录。
- 自动生成错词本，学习数据保存在浏览器本地。
- 可在“30 词重点学习库”和“2354 词 CEFR B1 扩展库”之间切换、搜索并查看词量。
- 精读分为“阅读精读”和“听力精读”两个独立子模块，切换后显示不同内容，保存的资料也不会混用。
- 阅读精读内置一篇 PET 难度短文，支持导入并保存 TXT、DOCX、PDF 提取出的文字。
- 听力精读可选择 B1 Preliminary for Schools 与 B1 Preliminary 两套公开样题。点击“下载并导入原文 PDF”会下载原始 PDF、自动解析保存原文并关联对应官网音频，刷新后可恢复已选资料。也可手动导入自己的音频与原文。
- 精读支持朗读全文、逐句朗读和 0.75x、1x、1.25x、1.5x 倍速；听力音频也可调速。
- 听力原文的每句话可以配置原版音频句首、句尾时间；配置按账号和资料自动保存。
- 支持原版音频单句定位、单句循环，以及自动挖空、完整句子核对的听写训练。
- 支持导出和恢复 JSON 学习备份。
- PWA 离线缓存，适配 iPhone 和华为 MatePad Air。

## 本地运行

需要 Node.js 22 或更高版本、pnpm 10 或更高版本。

```powershell
pnpm install --frozen-lockfile
pnpm dev
```

浏览器打开 `http://localhost:5173`。

## 检查命令

```powershell
pnpm build
pnpm exec eslint src/pet src/main.tsx
pnpm exec playwright test --config=playwright.pet.config.ts
```

## 当前边界

- 发音来自 iPhone 或华为设备自带的系统语音，不包含第三方版权音频。
- 正式账号使用 Supabase Auth 验证；数据库配置完成后同步学习进度、错词、计划和语音设置。同步状态以“我的”页面提示为准。
- 导入的精读原文和听力时间轴仍只存在当前浏览器。清除数据前请保留原文件，并导出学习 JSON 备份。
- 音频文件只在当前页面会话中使用，刷新后需重新选择；扫描版 PDF 暂不支持 OCR。
- Cambridge 官方音频采用官网在线播放，不复制到应用仓库；离线时需导入已下载的个人音频。两套公开样题 PDF 保留原文件及来源信息，随应用提供，以避免官网 CORS 限制；见 `public/listening/ATTRIBUTION.md`。
- 音频自动加载不等于自动出声，需点击播放。任意自选 PDF 不一定能确定对应的官方音频，未识别资料会提示导入配套录音，避免错误匹配。
- PDF/TXT 原文没有自带时间轴，需要首次使用时人工记录每句话的句首和句尾；之后会自动保存。
- 本地预览不注册 PWA 缓存，确保开发更新可以直接刷新；正式发布版本仍支持离线缓存。
- 2354 词扩展库是开放许可的 CEFR B1 词表，不是 Cambridge 官方 PET 完整词表，来源和许可见 `public/data/ATTRIBUTION.md`。

## 个人选词队列

- 核心和扩展词库均可标记“认识 / 不认识”，默认未标记的词不自动进入新词计划。
- “认识”列表支持恢复到“不认识”；相同拼写的核心词和扩展词共用标记。
- 今日尚未学习的名额及本周期后续新词日根据队列更新；已完成任务与过去日期保留。恢复的已学词不在同一周期重复安排，可在后续周期重新学习。
- 每个账户独立保存标记和已选扩展词，备份导出包含这些数据；扩展词支持学习卡、听写和错词本。
- 扩展库暂无人工校对的音节拆分，使用整词朗读。含扩展词的离线短文采用英语社团写作模板展示目标词；它不是 AI 根据词义创作的情节文章。

- 更新选词后需点击词库中的“增加”，再统一更新学习计划并进入学习页；未点击时标记作为草稿保存，不改变现有计划。重复点击不会重复安排同一周期内已学的单词。

- 跨周期复习：第 6 天合并往期实际学过且仍薄弱的词与本期薄弱词，按最新评分去重、不认识优先，并分到第 6、7 天。漏做复习或隔几个周期再回来，未掌握词仍继续结转；已认识的词不再进入后续复习。不把未学过的词算作复习词。

- 华为兼容发音：检测到华为浏览器时，词库单词、核心例句及音节优先使用内置美式合成音频；单词旁也提供“兼容音频（美式）”按钮。音频按需加载，首次播放需要网络。动态文章及未录制文本仍依赖设备英语语音引擎，失败时显示提示。
